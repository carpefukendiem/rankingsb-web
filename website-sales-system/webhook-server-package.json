{
  "name": "website-sales-webhook-server",
  "version": "1.0.0",
  "description": "Stripe webhook handler for $500 website sales",
  "main": "scripts/stripe-webhook-handler.js",
  "scripts": {
    "start": "node scripts/stripe-webhook-handler.js",
    "dev": "nodemon scripts/stripe-webhook-handler.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
