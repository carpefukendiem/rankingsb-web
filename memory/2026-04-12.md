- Added 30 SEO pages to `projects/rankingsb/web/my-app`: 18 blog posts (markdown + JSON-LD), 7 industry pages (markdown + JSON-LD), 5 location pages (markdown + JSON-LD).
- Extraction script: `scripts/extract-seo-content.mjs` reads `new-content-4:10:26/*.md` and schema folder; outputs `content/{blog,industries,locations}/*.md` and `lib/seo-graph-schemas.json` (canonical `https://rankingsb.com`, logo `logo-new.webp`, dates 2026-04-12).
- Dependencies: `react-markdown`, `remark-gfm`. Legacy blog JSX moved to `app/blog/[slug]/legacy-blog-posts.tsx`.
