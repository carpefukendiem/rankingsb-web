## SyncLocal (branch: synclocal)

- Added SyncLocal under `projects/rankingsb/web/my-app/`: Supabase migrations in `supabase/migrations/`, feature code in `lib/synclocal/`, admin at `/admin/synclocal`, APIs under `app/api/synclocal/`, `SiteChrome` so `/admin/*` skips marketing chrome, Vercel cron + `.env.local.example` (whitelisted in `.gitignore`).
- `npm run build` in `my-app` passes.
